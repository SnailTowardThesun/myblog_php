general:
    branches:
        only:
            - master
machine:
    php:
        version: 7.0.4
    services:
        - mysql

test:
    override:
        - (echo "ready to run test scripts")
        - (cd trunk && sh configure)
